{
  "name": "@groove-dev/desktop",
  "version": "0.27.71",
  "description": "Groove Desktop — one-click install for non-technical users",
  "author": "Groove Dev <hello@groovedev.ai> (https://groovedev.ai)",
  "license": "FSL-1.1-Apache-2.0",
  "type": "module",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "prebuild": "node bundle-daemon.js",
    "build": "electron-builder",
    "predist": "node bundle-daemon.js",
    "dist": "electron-builder --mac --win --linux",
    "dist:mac": "rm -rf /tmp/groove-dist && electron-builder --mac -c.directories.output=/tmp/groove-dist && mkdir -p dist && cp /tmp/groove-dist/*.dmg /tmp/groove-dist/*.zip /tmp/groove-dist/*.blockmap /tmp/groove-dist/*.yml dist/ 2>/dev/null; true",
    "test:local": "rm -rf /tmp/groove-test && node bundle-daemon.js && CSC_IDENTITY_AUTO_DISCOVERY=false npx electron-builder --mac --arm64 --config.mac.notarize=false -c.directories.output=/tmp/groove-test && open /tmp/groove-test/*.dmg"
  },
  "dependencies": {
    "@groove-dev/daemon": "*",
    "electron-updater": "^6.0.0"
  },
  "devDependencies": {
    "electron": "41.2.0",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "ai.groovedev.groove",
    "productName": "Groove",
    "asar": true,
    "files": [
      "main.js",
      "preload.cjs",
      "daemon-bridge.js",
      "assets/**"
    ],
    "extraResources": [
      {
        "from": ".daemon-bundle",
        "to": "daemon",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../gui/dist",
        "to": "gui"
      }
    ],
    "afterPack": "./afterPack.cjs",
    "artifactName": "Groove-${os}-${arch}.${ext}",
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "x64",
            "arm64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ],
      "category": "public.app-category.developer-tools",
      "icon": "assets/icon.png",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "entitlements.mac.plist",
      "entitlementsInherit": "entitlements.mac.plist",
      "notarize": true
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ],
      "icon": "assets/icon.png"
    },
    "linux": {
      "target": [
        {
          "target": "AppImage",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ],
      "category": "Development",
      "icon": "assets/icon.png"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false
    },
    "dmg": {
      "contents": [
        {
          "x": 130,
          "y": 220
        },
        {
          "x": 410,
          "y": 220,
          "type": "link",
          "path": "/Applications"
        }
      ]
    },
    "publish": {
      "provider": "github",
      "owner": "grooveai-dev",
      "repo": "groove"
    }
  }
}
